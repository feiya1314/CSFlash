package com.yufeiblog.cassandra.test;

public class RepairTest extends BaseTest{
    @Override
    protected void test() {

    }

    //system.size_estimates 表中 里面记录了每个表在每个token段上大约有多少分区key（partitions_count）以及每个分区key的大小（mean_partition_size）
}
